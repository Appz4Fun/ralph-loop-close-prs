1. **Pre commit step**
   - Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.

2. **Submit PR**
   - Submit the PR using `gh pr create` with `run_in_bash_session`. I will use the `submit` tool to do this.
